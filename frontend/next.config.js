/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone',
  /* Allow rendering external svg or standard optimization settings if needed */
};

module.exports = nextConfig;
