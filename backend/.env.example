# Database Connection (local development outside Docker uses localhost, inside compose uses db)
DATABASE_URL=postgresql+asyncpg://postgres:postgrespassword@localhost:5432/system_design_generator

# Groq API Key (Sign up and get it for free from console.groq.com)
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
